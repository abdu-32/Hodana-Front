# Copy to .env.local in this directory and adjust as needed.
#
# Points at the Prism mock server (docker-compose.yml -> mock-api) by
# default, so this app runs standalone with zero backend dependency.
# Point this at a real Django instance (local or staging) once you want
# to test against live data.
NEXT_PUBLIC_API_URL=http://localhost:4010
